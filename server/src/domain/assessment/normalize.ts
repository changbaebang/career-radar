// The normalization rule lives in the shared package (the widget uses it to label citations);
// this module keeps the server-side import path stable.
export { normalizeEvidence } from "@career-radar/shared";
